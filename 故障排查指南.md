# 🔧 心语时光 - 故障排查与修复指南

## 已修复的 Bug

### ✅ Bug 1: 日期格式错误
**文件**: `app/main/utils.py` 第 44 行  
**问题**: `'%Y-%m-% d'` (有空格)  
**修复**: `'%Y-%m-%d'`  
**状态**: 已修复

### ✅ Bug 2: 目录权限问题
**文件**: `app/__init__.py` `ensure_directories` 函数  
**问题**: 创建目录时未设置权限，缺少错误处理  
**修复**: 添加 `mode=0o755` 和异常处理  
**状态**: 已修复

### ✅ Bug 3: 生产环境配置检查过早
**文件**: `app/config.py`  
**问题**: 在类定义时就检查环境变量  
**修复**: 移除过早的检查  
**状态**: 已修复

### ✅ Bug 4: 环境变量加载时机
**文件**: `scripts/init_db.py`, `scripts/create_admin.py`  
**问题**: 在导入 app 后才加载 .env  
**修复**: 在导入 app 之前加载 .env  
**状态**: 已修复

---

## 🚨 当前错误：SQLite 数据库无法创建

### 错误信息
```
sqlite3.OperationalError: unable to open database file
```

### 原因分析
1. instance 目录权限不足
2. 磁盘空间不足
3. SELinux 权限限制（CentOS/RHEL）

---

## ✅ 完整解决方案

### Step 1: 检查目录权限

```bash
# 1. 检查当前目录权限
ls -la

# 2. 检查 instance 目录
ls -la instance/

# 3. 创建并设置权限
mkdir -p instance
chmod 755 instance

# 4. 验证权限
ls -ld instance/
# 应该显示: drwxr-xr-x
```

### Step 2: 检查磁盘空间

```bash
# 检查磁盘空间
df -h

# 检查当前目录可用空间
df -h .

# 检查 inode 使用情况（文件数量限制）
df -i
```

### Step 3: 检查 SELinux（如果是 CentOS/RHEL）

```bash
# 查看 SELinux 状态
getenforce

# 如果是 Enforcing，可以临时禁用测试
sudo setenforce 0

# 或者添加 SELinux 规则
sudo chcon -R -t httpd_sys_rw_content_t instance/
```

### Step 4: 检查文件所有者

```bash
# 查看当前用户
whoami

# 查看文件所有者
ls -la

# 如果所有者不对，修改
sudo chown -R $USER:$USER .
```

### Step 5: 手动创建数据库文件测试

```bash
# 测试是否可以创建文件
touch instance/test.db
ls -la instance/

# 如果可以创建，删除测试文件
rm instance/test.db
```

---

## 🚀 重新初始化步骤

完成上述检查后：

```bash
# 1. 确保在正确的目录
pwd
# 应该在项目根目录，如 /home/opc/rein-tini

# 2. 激活虚拟环境
source venv/bin/activate

# 3. 验证环境变量
python3 << 'EOF'
from dotenv import load_dotenv
import os
load_dotenv()
print("FLASK_ENV:", os.environ.get('FLASK_ENV'))
print("SECRET_KEY:", os.environ.get('SECRET_KEY')[:20] + "..." if os.environ.get('SECRET_KEY') else None)
EOF

# 4. 清理之前的数据（可选）
rm -f instance/heartmoments.db*

# 5. 重新初始化
python scripts/init_db.py

# 6. 创建管理员
python scripts/create_admin.py

# 7. 测试启动
python app.py
```

---

## 🔍 详细诊断命令

如果问题仍然存在，运行以下诊断：

```bash
# 诊断脚本
cat > diagnose.sh << 'EOF'
#!/bin/bash
echo "=== 系统信息 ==="
uname -a
echo ""

echo "=== Python 版本 ==="
python3 --version
echo ""

echo "=== 当前目录 ==="
pwd
echo ""

echo "=== 目录权限 ==="
ls -la | grep -E "instance|uploads|logs|backups"
echo ""

echo "=== 磁盘空间 ==="
df -h .
echo ""

echo "=== Inode 使用 ==="
df -i .
echo ""

echo "=== SELinux 状态 ==="
getenforce 2>/dev/null || echo "SELinux not installed"
echo ""

echo "=== 当前用户 ==="
whoami
id
echo ""

echo "=== 环境变量 ==="
env | grep -E "FLASK|SECRET|DATABASE" || echo "No Flask env vars"
echo ""

echo "=== .env 文件 ==="
if [ -f .env ]; then
    echo ".env exists"
    head -n 5 .env
else
    echo ".env NOT FOUND"
fi
echo ""

echo "=== Python 包 ==="
pip list | grep -E "Flask|SQLAlchemy|Pillow"
echo ""
EOF

chmod +x diagnose.sh
./diagnose.sh
```

---

## 🐛 其他潜在问题检查

### 检查数据库 URI 配置

```python
# 运行这个 Python 脚本检查配置
python3 << 'EOF'
import os
import sys
from dotenv import load_dotenv

load_dotenv()
sys.path.insert(0, '.')

from app.config import get_config

config = get_config()
print("数据库 URI:", config.SQLALCHEMY_DATABASE_URI)
print("Base Dir:", config.BASE_DIR)
print("Upload Folder:", config.UPLOAD_FOLDER)

# 检查 instance 目录路径
import os.path
db_path = config.SQLALCHEMY_DATABASE_URI.replace('sqlite:///', '')
print("数据库文件路径:", db_path)
print("目录是否存在:", os.path.exists(os.path.dirname(db_path)))
EOF
```

### 检查 Python 导入

```python
# 测试导入是否正常
python3 << 'EOF'
import sys
sys.path.insert(0, '.')

try:
    print("Testing imports...")
    from dotenv import load_dotenv
    print("✓ dotenv")
    
    load_dotenv()
    print("✓ .env loaded")
    
    from app import create_app
    print("✓ create_app imported")
    
    app = create_app()
    print("✓ app created")
    
    print("\nAll imports successful!")
except Exception as e:
    print(f"\n✗ Error: {e}")
    import traceback
    traceback.print_exc()
EOF
```

---

## 📋 问题排查清单

完成以下检查：

- [ ] instance 目录存在且权限为 755
- [ ] 磁盘空间充足（至少 1GB 可用）
- [ ] .env 文件在项目根目录
- [ ] FLASK_ENV 设置为 development
- [ ] SECRET_KEY 已设置
- [ ] 虚拟环境已激活
- [ ] 所有依赖已安装
- [ ] SELinux 不阻止文件创建
- [ ] 当前用户有写权限
- [ ] 没有其他进程锁定数据库

---

## 💡 常见解决方案速查

### 权限拒绝
```bash
chmod -R 755 .
chown -R $USER:$USER .
```

### 磁盘空间不足
```bash
# 清理临时文件
rm -rf /tmp/*
# 清理 pip 缓存
pip cache purge
```

### SELinux 阻止
```bash
sudo setenforce 0  # 临时禁用
# 或
sudo chcon -R -t httpd_sys_rw_content_t .
```

### 数据库锁定
```bash
# 删除锁文件
rm -f instance/*.db-shm
rm -f instance/*.db-wal
```

---

## 🆘 如果仍然失败

1. **完整重置**

```bash
# 备份配置
cp .env .env.backup

# 删除虚拟环境
rm -rf venv

# 删除数据库和临时文件
rm -rf instance logs/__pycache__ app/__pycache__

# 重新创建虚拟环境
python3 -m venv venv
source venv/bin/activate

# 重新安装依赖
pip install --upgrade pip
pip install -r requirements.txt

# 恢复配置
cp .env.backup .env

# 重新初始化
python scripts/init_db.py
```

2. **使用绝对路径**

修改 `.env`：
```env
DATABASE_URI=sqlite:////home/opc/rein-tini/instance/heartmoments.db
```

3. **检查系统日志**

```bash
# 查看系统日志
sudo journalctl -xe | tail -50

# 查看 SELinux 日志
sudo ausearch -m avc -ts recent
```

---

**完成所有检查后，应该就能成功初始化数据库了！** 💪

