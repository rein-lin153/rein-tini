# 🚨 HeartMoments 服务启动失败诊断

**错误**：`Job for heartmoments.service failed because the control process exited with error code`

---

## 🔍 立即执行以下命令查看错误详情

```bash
# 1. 查看服务状态
sudo systemctl status heartmoments

# 2. 查看详细日志（最重要！）
sudo journalctl -u heartmoments -n 50 --no-pager

# 3. 查看 Gunicorn 错误日志
tail -n 50 /home/opc/rein-tini/logs/gunicorn-error.log
```

---

## 🛠️ 常见原因和解决方案

### ❌ 原因1：.env 文件不存在或配置错误

**检查**：
```bash
ls -la /home/opc/rein-tini/.env
cat /home/opc/rein-tini/.env | grep -E "SECRET_KEY|DATABASE_URI"
```

**解决**：
```bash
cd /home/opc/rein-tini

# 如果 .env 不存在
if [ ! -f .env ]; then
    cp env.example .env
    nano .env
    # 修改 SECRET_KEY 和其他必要配置
fi

# 生成新的 SECRET_KEY
python3 -c "import os; print(os.urandom(24).hex())"

# 确保 DATABASE_URI 使用绝对路径（4个斜杠）
nano .env
# DATABASE_URI=sqlite:////home/opc/rein-tini/instance/heartmoments.db
```

---

### ❌ 原因2：虚拟环境不存在或损坏

**检查**：
```bash
ls -la /home/opc/rein-tini/venv/bin/python
/home/opc/rein-tini/venv/bin/python --version
```

**解决**：
```bash
cd /home/opc/rein-tini

# 删除旧的虚拟环境
rm -rf venv

# 重新创建
python3 -m venv venv

# 激活并安装依赖
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
```

---

### ❌ 原因3：Gunicorn 未安装

**检查**：
```bash
/home/opc/rein-tini/venv/bin/gunicorn --version
```

**解决**：
```bash
cd /home/opc/rein-tini
source venv/bin/activate
pip install gunicorn
```

---

### ❌ 原因4：文件权限问题

**检查**：
```bash
ls -la /home/opc/rein-tini/
ls -la /home/opc/rein-tini/instance/
ls -la /home/opc/rein-tini/logs/
```

**解决**：
```bash
cd /home/opc/rein-tini

# 修复所有权
sudo chown -R opc:opc /home/opc/rein-tini

# 修复目录权限
chmod 755 /home/opc/rein-tini
chmod 755 /home/opc/rein-tini/instance
chmod 755 /home/opc/rein-tini/logs
chmod 755 /home/opc/rein-tini/uploads

# 确保日志目录存在
mkdir -p /home/opc/rein-tini/logs
```

---

### ❌ 原因5：数据库未初始化

**检查**：
```bash
ls -la /home/opc/rein-tini/instance/heartmoments.db
```

**解决**：
```bash
cd /home/opc/rein-tini

# 确保 instance 目录存在
mkdir -p instance
chmod 755 instance

# 初始化数据库
source venv/bin/activate
python scripts/init_db.py
```

---

### ❌ 原因6：wsgi.py 文件问题

**检查**：
```bash
cat /home/opc/rein-tini/wsgi.py

# 测试是否能导入 app
cd /home/opc/rein-tini
source venv/bin/activate
python -c "from wsgi import app; print('OK')"
```

**解决**：
```bash
# 如果导入失败，检查 Python 路径
cd /home/opc/rein-tini
source venv/bin/activate
python -c "import sys; print('\n'.join(sys.path))"
```

---

### ❌ 原因7：SELinux 阻止

**检查**：
```bash
getenforce
sudo ausearch -m avc -ts recent
```

**解决**：
```bash
# 临时禁用 SELinux（测试用）
sudo setenforce 0

# 如果这样能启动，说明是 SELinux 问题
# 永久解决：
sudo setsebool -P httpd_can_network_connect 1

# 或者永久禁用 SELinux（不推荐）
sudo sed -i 's/SELINUX=enforcing/SELINUX=disabled/' /etc/selinux/config
```

---

### ❌ 原因8：端口或 Socket 被占用

**检查**：
```bash
ls -la /home/opc/rein-tini/*.sock
sudo lsof /home/opc/rein-tini/heartmoments.sock
```

**解决**：
```bash
# 删除旧的 socket 文件
rm -f /home/opc/rein-tini/heartmoments.sock

# 杀死可能残留的进程
sudo pkill -9 gunicorn
```

---

### ❌ 原因9：Systemd 服务配置错误

**检查**：
```bash
cat /etc/systemd/system/heartmoments.service
```

**解决**：
```bash
# 重新复制配置文件
sudo cp /home/opc/rein-tini/deploy/heartmoments.service /etc/systemd/system/

# 重新加载
sudo systemctl daemon-reload
```

---

## 🔧 完整诊断脚本

执行以下脚本收集所有诊断信息：

```bash
cd /home/opc/rein-tini
bash deploy/故障排查.sh
```

---

## 📋 标准修复流程（推荐）

```bash
# 1. 停止服务（如果在运行）
sudo systemctl stop heartmoments

# 2. 清理旧文件
rm -f /home/opc/rein-tini/heartmoments.sock
sudo pkill -9 gunicorn

# 3. 检查并修复权限
cd /home/opc/rein-tini
sudo chown -R opc:opc .
chmod 755 . instance logs uploads
mkdir -p logs instance uploads/photos uploads/thumbs

# 4. 检查 .env 文件
if [ ! -f .env ]; then
    echo "错误: .env 文件不存在！"
    exit 1
fi

# 5. 检查虚拟环境
if [ ! -f venv/bin/python ]; then
    echo "重建虚拟环境..."
    python3 -m venv venv
    source venv/bin/activate
    pip install -r requirements.txt
fi

# 6. 测试能否启动
source venv/bin/activate
python -c "from wsgi import app; print('✓ wsgi.py 导入成功')"

# 7. 手动启动 Gunicorn 测试
cd /home/opc/rein-tini
source venv/bin/activate
gunicorn --bind 127.0.0.1:8000 --workers 1 "wsgi:app"
# Ctrl+C 停止测试

# 8. 如果手动启动成功，重新加载 systemd
sudo systemctl daemon-reload
sudo systemctl start heartmoments

# 9. 检查状态
sudo systemctl status heartmoments
```

---

## 🎯 快速诊断命令（一键执行）

```bash
echo "=== 1. 检查 .env 文件 ==="
ls -la /home/opc/rein-tini/.env

echo "=== 2. 检查虚拟环境 ==="
ls -la /home/opc/rein-tini/venv/bin/python

echo "=== 3. 检查 Gunicorn ==="
/home/opc/rein-tini/venv/bin/gunicorn --version

echo "=== 4. 检查目录权限 ==="
ls -la /home/opc/rein-tini/ | head -20

echo "=== 5. 检查数据库 ==="
ls -la /home/opc/rein-tini/instance/

echo "=== 6. 测试 wsgi ==="
cd /home/opc/rein-tini && source venv/bin/activate && python -c "from wsgi import app; print('OK')"

echo "=== 7. 查看服务日志 ==="
sudo journalctl -u heartmoments -n 30 --no-pager

echo "=== 8. 查看 SELinux ==="
getenforce
```

---

## 💡 下一步

1. **先执行上面的诊断命令**，找出具体错误
2. **根据错误信息**，选择对应的解决方案
3. **如果还是不行**，提供诊断信息寻求帮助

关键是看 `sudo journalctl -u heartmoments -n 50` 的输出！

