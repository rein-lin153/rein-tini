# 音乐播放器故障排查指南

## 问题诊断步骤

### 1. 检查配置

在 `.env` 文件中添加（如果还没有）：
```env
ENABLE_BACKGROUND_MUSIC=True
```

### 2. 检查音乐文件位置

音乐文件必须放在：`app/static/music/` 目录

**Windows路径示例：**
```
C:\Users\l1531\Documents\Test\Romance\app\static\music\你的音乐.mp3
```

### 3. 检查文件格式

支持的格式：`.mp3`, `.wav`, `.ogg`, `.m4a`, `.flac`

### 4. 检查浏览器控制台

1. 打开网站
2. 按 `F12` 打开开发者工具
3. 查看 `Console` 标签页
4. 查找以下信息：
   - `音乐列表API响应:` - 应该显示音乐列表
   - `加载到 X 首歌曲` - 应该显示找到的歌曲数量
   - 如果有错误，会显示红色错误信息

### 5. 测试API接口

在浏览器中访问：
```
http://你的域名/api/music/list
```

应该返回JSON格式的音乐列表，例如：
```json
{
  "success": true,
  "music_list": [
    {
      "filename": "song.mp3",
      "title": "song",
      "artist": "未知艺术家",
      "url": "/static/music/song.mp3"
    }
  ],
  "count": 1
}
```

### 6. 检查服务器日志

查看服务器日志，应该能看到：
- `音乐文件夹路径: ...`
- `音乐文件夹中的文件: [...]`
- `找到 X 首音乐`

### 7. 常见问题

#### 问题1：播放器不显示
**原因：** `ENABLE_BACKGROUND_MUSIC` 未启用
**解决：** 在 `.env` 中设置 `ENABLE_BACKGROUND_MUSIC=True`

#### 问题2：显示"暂无音乐"
**原因：** 音乐文件不在正确位置或格式不支持
**解决：** 
- 检查文件是否在 `app/static/music/` 目录
- 检查文件扩展名是否支持
- 检查文件名是否有特殊字符

#### 问题3：无法播放
**原因：** 浏览器自动播放策略限制
**解决：** 需要用户点击播放按钮（这是浏览器安全策略）

#### 问题4：API返回空列表
**原因：** 音乐文件夹路径配置错误
**解决：** 检查 `app/config.py` 中的 `MUSIC_FOLDER` 配置

### 8. 手动测试步骤

1. **确认文件存在：**
   ```bash
   # 在项目根目录
   ls app/static/music/
   # 或 Windows
   dir app\static\music
   ```

2. **测试API：**
   ```bash
   curl http://localhost:5000/api/music/list
   ```

3. **检查配置：**
   - 打开浏览器控制台
   - 查看是否有JavaScript错误
   - 查看网络请求，检查 `/api/music/list` 的响应

### 9. 文件命名建议

推荐格式：`艺术家 - 歌曲名.mp3`

例如：
- `Taylor Swift - Love Story.mp3`
- `周杰伦 - 告白气球.mp3`

如果不使用 " - " 分隔，文件名将作为歌曲标题，艺术家显示为"未知艺术家"。

### 10. 重启服务

修改配置后，需要重启Flask应用：
```bash
# 如果使用systemd
sudo systemctl restart heartmoments

# 如果直接运行
# 停止当前进程，然后重新启动
```

## 如果仍然无法解决问题

1. 检查浏览器控制台的完整错误信息
2. 检查服务器日志的完整错误信息
3. 确认音乐文件确实存在于 `app/static/music/` 目录
4. 确认文件权限正确（可读）
5. 尝试使用不同的音乐文件
6. 检查网络请求，确认API正常响应

