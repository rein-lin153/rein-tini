# 🚀 生产环境配置指南 - 自定义路径版本

**项目路径**：`/home/opc/rein-tini`  
**用户**：`opc`  
**适用系统**：CentOS 7+

---

## ✅ Step 1: 确认项目结构

```bash
cd /home/opc/rein-tini
ls -la
```

应该看到：
```
app/
scripts/
static/
uploads/
instance/
.env
requirements.txt
wsgi.py
...
```

---

## 🔧 Step 2: 配置环境变量

### 编辑 `.env` 文件

```bash
cd /home/opc/rein-tini
nano .env
```

**关键配置**（确保使用绝对路径）：

```bash
# ============== 核心配置 ==============
SECRET_KEY=你的密钥（必须修改！）
FLASK_ENV=production
FLASK_APP=wsgi.py

# ============== 数据库配置 ==============
# 使用绝对路径！
DATABASE_URI=sqlite:////home/opc/rein-tini/instance/heartmoments.db

# ============== 文件上传配置 ==============
UPLOAD_FOLDER=/home/opc/rein-tini/uploads
MAX_CONTENT_LENGTH=5242880

# ============== 情侣信息 ==============
COUPLE_NAME_1=Rein
COUPLE_NAME_2=Nana
TOGETHER_DATE=2023-01-14

# ============== 日志配置 ==============
LOG_FILE=/home/opc/rein-tini/logs/heartmoments.log
LOG_LEVEL=INFO

# ============== 备份配置 ==============
BACKUP_DIR=/home/opc/rein-tini/backups
```

**生成 SECRET_KEY**：
```bash
python3 -c "import os; print(os.urandom(24).hex())"
```

---

## 📦 Step 3: 安装依赖

```bash
cd /home/opc/rein-tini

# 创建虚拟环境
python3 -m venv venv

# 激活虚拟环境
source venv/bin/activate

# 升级 pip
pip install --upgrade pip

# 安装依赖
pip install -r requirements.txt
```

---

## 🗄️ Step 4: 初始化数据库

```bash
cd /home/opc/rein-tini
source venv/bin/activate

# 初始化数据库
python scripts/init_db.py

# 创建管理员用户
python scripts/create_admin.py
```

**按提示输入**：
- 用户名：`admin` 或 `rein`
- 显示名称：`Rein`
- 密码：（设置一个强密码）

---

## 🔐 Step 5: 设置文件权限

```bash
cd /home/opc/rein-tini

# 确保目录存在
mkdir -p instance uploads/photos uploads/thumbs logs backups

# 设置权限（重要！）
chmod 755 /home/opc/rein-tini
chmod 755 instance uploads logs backups
chmod 755 uploads/photos uploads/thumbs

# 确保 opc 用户拥有所有文件
chown -R opc:opc /home/opc/rein-tini

# 检查权限
ls -la
ls -la instance/
ls -la uploads/
```

---

## ⚙️ Step 6: 创建 Systemd 服务

### 创建 Gunicorn 服务文件

```bash
sudo nano /etc/systemd/system/heartmoments.service
```

**内容**（直接复制粘贴）：

```ini
[Unit]
Description=HeartMoments Gunicorn Service
After=network.target

[Service]
Type=notify
User=opc
Group=opc
RuntimeDirectory=gunicorn
WorkingDirectory=/home/opc/rein-tini
Environment="PATH=/home/opc/rein-tini/venv/bin"
EnvironmentFile=/home/opc/rein-tini/.env
ExecStart=/home/opc/rein-tini/venv/bin/gunicorn \
    --workers 2 \
    --threads 2 \
    --worker-class gthread \
    --worker-tmp-dir /dev/shm \
    --bind unix:/home/opc/rein-tini/heartmoments.sock \
    --access-logfile /home/opc/rein-tini/logs/gunicorn-access.log \
    --error-logfile /home/opc/rein-tini/logs/gunicorn-error.log \
    --log-level info \
    --timeout 120 \
    --graceful-timeout 30 \
    --keep-alive 5 \
    "wsgi:app"
ExecReload=/bin/kill -s HUP $MAINPID
KillMode=mixed
KillSignal=SIGTERM
PrivateTmp=true
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

### 启动服务

```bash
# 重载 systemd 配置
sudo systemctl daemon-reload

# 启动服务
sudo systemctl start heartmoments

# 检查状态
sudo systemctl status heartmoments

# 设置开机自启
sudo systemctl enable heartmoments
```

**查看日志**（如果启动失败）：
```bash
sudo journalctl -u heartmoments -f
tail -f /home/opc/rein-tini/logs/gunicorn-error.log
```

---

## 🌐 Step 7: 配置 Nginx

### 创建 Nginx 配置文件

```bash
sudo nano /etc/nginx/conf.d/heartmoments.conf
```

**内容**（直接复制粘贴）：

```nginx
# HTTP 服务器配置
server {
    listen 80;
    server_name 你的域名或IP;  # 例如：example.com 或 217.142.232.70
    
    charset utf-8;
    client_max_body_size 10M;
    
    # 访问日志
    access_log /var/log/nginx/heartmoments_access.log;
    error_log /var/log/nginx/heartmoments_error.log;
    
    # 静态文件
    location /static/ {
        alias /home/opc/rein-tini/app/static/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
    
    # 上传的文件
    location /uploads/ {
        alias /home/opc/rein-tini/uploads/;
        expires 7d;
        add_header Cache-Control "public";
    }
    
    # 代理到 Gunicorn
    location / {
        proxy_pass http://unix:/home/opc/rein-tini/heartmoments.sock;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}
```

### 测试并重启 Nginx

```bash
# 测试配置
sudo nginx -t

# 如果测试通过，重启 Nginx
sudo systemctl restart nginx

# 检查状态
sudo systemctl status nginx
```

---

## 🔍 Step 8: 验证部署

### 1. 检查服务状态

```bash
# Gunicorn 服务
sudo systemctl status heartmoments

# Nginx 服务
sudo systemctl status nginx

# 检查 socket 文件
ls -la /home/opc/rein-tini/heartmoments.sock
```

### 2. 测试访问

**浏览器访问**：
```
http://你的服务器IP
```

例如：`http://217.142.232.70`

### 3. 查看日志

```bash
# Gunicorn 日志
tail -f /home/opc/rein-tini/logs/gunicorn-access.log
tail -f /home/opc/rein-tini/logs/gunicorn-error.log

# Nginx 日志
sudo tail -f /var/log/nginx/heartmoments_access.log
sudo tail -f /var/log/nginx/heartmoments_error.log

# 应用日志
tail -f /home/opc/rein-tini/logs/heartmoments.log
```

---

## 🐛 常见问题排查

### 问题 1：502 Bad Gateway

**可能原因**：
1. Gunicorn 服务未启动
2. Socket 文件权限问题
3. SELinux 阻止

**解决方案**：

```bash
# 检查 Gunicorn 状态
sudo systemctl status heartmoments
sudo journalctl -u heartmoments -n 50

# 检查 socket 文件
ls -la /home/opc/rein-tini/heartmoments.sock

# 临时禁用 SELinux（排查用）
sudo setenforce 0

# 永久禁用 SELinux（如果确认是 SELinux 问题）
sudo sed -i 's/SELINUX=enforcing/SELINUX=disabled/' /etc/selinux/config

# 或者配置 SELinux 规则（推荐）
sudo setsebool -P httpd_can_network_connect 1
sudo chcon -R -t httpd_sys_content_t /home/opc/rein-tini/app/static
sudo chcon -R -t httpd_sys_content_t /home/opc/rein-tini/uploads
```

### 问题 2：静态文件 403 Forbidden

**解决方案**：

```bash
# 检查目录权限
namei -l /home/opc/rein-tini/app/static

# 确保所有父目录可访问
chmod 755 /home
chmod 755 /home/opc
chmod 755 /home/opc/rein-tini
chmod -R 755 /home/opc/rein-tini/app/static
chmod -R 755 /home/opc/rein-tini/uploads

# 设置 SELinux 上下文
sudo chcon -R -t httpd_sys_content_t /home/opc/rein-tini/app/static
sudo chcon -R -t httpd_sys_content_t /home/opc/rein-tini/uploads
```

### 问题 3：数据库无法写入

**解决方案**：

```bash
# 检查 instance 目录权限
ls -la /home/opc/rein-tini/instance

# 确保 opc 用户有写权限
chown -R opc:opc /home/opc/rein-tini/instance
chmod 755 /home/opc/rein-tini/instance
chmod 644 /home/opc/rein-tini/instance/heartmoments.db

# 测试写权限
touch /home/opc/rein-tini/instance/test.txt
rm /home/opc/rein-tini/instance/test.txt
```

### 问题 4：上传图片失败

**解决方案**：

```bash
# 检查 uploads 目录
ls -la /home/opc/rein-tini/uploads

# 设置权限
mkdir -p /home/opc/rein-tini/uploads/photos
mkdir -p /home/opc/rein-tini/uploads/thumbs
chown -R opc:opc /home/opc/rein-tini/uploads
chmod -R 755 /home/opc/rein-tini/uploads

# 检查磁盘空间
df -h
```

---

## 🔄 常用运维命令

### 重启服务

```bash
# 重启 Gunicorn
sudo systemctl restart heartmoments

# 重启 Nginx
sudo systemctl restart nginx

# 重启所有
sudo systemctl restart heartmoments nginx
```

### 查看日志

```bash
# 实时查看应用日志
tail -f /home/opc/rein-tini/logs/gunicorn-error.log

# 查看最近的错误
sudo journalctl -u heartmoments -n 100 --no-pager
```

### 更新代码

```bash
cd /home/opc/rein-tini

# 备份数据库
cp instance/heartmoments.db instance/heartmoments.db.backup

# 拉取最新代码
git pull

# 激活虚拟环境
source venv/bin/activate

# 更新依赖
pip install -r requirements.txt

# 重启服务
sudo systemctl restart heartmoments
```

---

## 📊 性能监控

### 检查内存使用

```bash
free -h
ps aux | grep gunicorn
```

### 检查磁盘使用

```bash
df -h
du -sh /home/opc/rein-tini/*
```

### 检查网络连接

```bash
sudo netstat -tlnp | grep nginx
sudo netstat -tlnp | grep gunicorn
```

---

## 🔐 安全加固（可选）

### 1. 配置防火墙

```bash
# 只允许 HTTP/HTTPS
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --permanent --add-service=https
sudo firewall-cmd --reload
```

### 2. 限制 SSH 访问

```bash
# 修改 SSH 端口（可选）
sudo nano /etc/ssh/sshd_config
# Port 2222

sudo systemctl restart sshd
```

### 3. 配置 Fail2ban（可选）

```bash
sudo yum install -y fail2ban
sudo systemctl start fail2ban
sudo systemctl enable fail2ban
```

---

## 🎉 完成检查清单

部署完成后，请检查以下项目：

- [ ] Gunicorn 服务正常运行
- [ ] Nginx 服务正常运行
- [ ] 网站可以通过浏览器访问
- [ ] 可以登录管理员账号
- [ ] 可以上传照片
- [ ] 可以发布日记
- [ ] 静态文件正常加载（CSS/JS）
- [ ] 图片可以正常查看
- [ ] 返回顶部按钮正常工作
- [ ] 日志正常记录

---

## 📞 需要帮助？

如果遇到问题，请提供以下信息：

```bash
# 收集诊断信息
echo "=== Gunicorn Status ==="
sudo systemctl status heartmoments

echo "=== Nginx Status ==="
sudo systemctl status nginx

echo "=== Recent Logs ==="
sudo journalctl -u heartmoments -n 20

echo "=== File Permissions ==="
ls -la /home/opc/rein-tini/
ls -la /home/opc/rein-tini/instance/

echo "=== Disk Space ==="
df -h

echo "=== SELinux Status ==="
getenforce
```

---

**部署完成！** 🎊

现在您的"心语时光"网站已经在生产环境中运行了！

**访问地址**：`http://你的服务器IP`

祝您使用愉快！💖

